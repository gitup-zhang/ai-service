version: "3.8"

services:
  ai-service:
    build: .
    container_name: ai-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped
